# OmniHarness 总任务看板（2026-09-13 · 全量盘点版）

> **定位：唯一的前进看板。** 自本板起，剩余任务一律在此跟踪；`REFACTOR_BOARD_2026-09-12.md`（P/T 批次记录 + 决策日志 D1–D9）与 `UPGRADE_BOARD_2026-09-12.md`（U 批次记录）降级为**历史账**——其中已完成任务与用户决策（D1–D9、口径、纪律）继续有效，剩余任务已全部并入本板。
> **盘点方法**：2026-09-13 全天 20+ 提交逐条核对 + `grep`/`Read` 源码实测（MCP 版本 / shellTool / permissionMode / promptInjectionGuard / CI job 内容 / 零测试模块 / a11y 标记）+ 两板对账 + `ARCHITECTURE_AND_GAP_2026-09-13.md` 差距清单对账。**不采信任何自我宣称。**
> 状态图例：⬜ 待办 · 🟡 进行中（有未提交改动） · ✅ 已完成 · ⏸️ 挂起（有明确外部条件） · ❌ 实测判负。
> 任务卡格式沿用两板标准（现状证据 / 动作 / 可证伪验收），完成判据 = 可证伪验收 + 四闸门（typecheck / check --strict / arch:gate / audit:maturity）全绿。

---

## 0. 盘点结论（一页判读）

1. **工程债清偿接近尾声**：P 系列 34 项中已结 27 项；T 系列今日（9-13）一次性收口 **T4 6/7 + T5 5/5**——验证闭环与训练信号两大主线从"待建"变为"主体落地"。
2. **三条旧短板已悄然闭合**（9-09 审计口径已过期，实测更正）：CI 已有 web job（`web:build`+`web:test`）；Rust `cargo fmt/clippy/test` 已进 CI；提示注入阻断已接线生产路径（`promptInjectionGuard` 接 `agent/config/cli`，高危命中即 blocked）。
3. **剩余硬缺口收敛**：兼容性——A 线可开工项已清空（A1/A2/A3 ✅ 2026-09-14），仅剩挂起中的 A4 注入度量（等数据集快照）；可信度——官方 SWE-bench / OS 沙箱真机（均挂起待外部条件）；Web UI 工程化——a11y 基线 / 增量流式 / 应用内对话框三件（D1/D2/D4 ✅ `de10f05`）与 **D3 e2e / D5 产物画廊 / F1 零测试模块（✅ 2026-09-14 `a0c860e`）均已收口**，D 线可开工项清空；目录收敛 ✅ 全部达成（arch:gate 目录告警 0）。**本批另修掉一个真实阻断缺陷**：`App.ts` 继承环境声明类 `ReactComponent` → 工作台自 C3 起在浏览器中无法挂载（详见 §5 第 7 条）。
4. **工作树状态（2026-09-14 02:45 复核）**：代码与看板已全部提交（本地 HEAD = `de10f05`）；并行会话的 `crates/omni-sdk-gen` 已入库（`77564a8`）；A3（`57f9708`）+ A2（`3e4131a`）+ UI 收口批次（`de10f05`）结项均已入库（pre-commit 门禁实跑通过），工作树干净，无 in-flight 未提交改动。
5. 挂起项全部有明确外部条件：官方跑分须 API key、注入度量须数据集、沙箱真机须 Linux/macOS 环境——除此之外**没有任务被"不知道怎么做"卡住**。
6. **sess_06f54aca（差距分析会话）对账完成（2026-09-14）**：该会话产出的差距报告 `ARCHITECTURE_AND_GAP_2026-09-13.md`（对标 14 个成熟同类）四大核心缺口与本板 A/B 线完全对应；其挂起的 in-flight 项（ports 重命名 49 个、临时调试文件 4 个）**均已在本板周期内清偿/清零**；《全能AI战士-Harness融合蓝图.md》确认废弃（回收站，悬空引用已清）。新增 **E4**（AST 图上下文压缩深化，战略硬骨头①）入板；F1 修正 ×4→×3（lsaEngine 已有测试）。

---

## 1. 对上午盘点（`ARCHITECTURE_AND_GAP_2026-09-13.md`）的更正

> 诚实记录：上午报告部分结论引自 9-09 审计，下午代码实测推翻以下五条。全景报告待下次修订时同步。

| 上午结论                       | 实测更正（9-13 下午）                                                                                                                                                                                                                                                                 |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| §7.3 "A2A 互操作客户端未建"    | **已建**：`a2a` 协议域全落地 + localhost 回环实测 100%（U6 ✅，串行 30 + 并发 5×50，p95 5ms/28ms）。剩余跨机/WebSocket 传输实测 **E2 已补齐**（2026-09-14 `e2ebea8`）：跨进程 HTTP / WS 各一例 + 真实子代理委托链路（子进程 pid 证伪），并修掉 `a2a` 装配未透传、断言形态不匹配等缺陷 |
| §7.1 "CI 不构建 web"           | **已闭**：CI `web` job 实跑 `npm run web:build` + `npm run web:test`；web 测试 3→**4** 个文件（今日新增挂载契约测试）                                                                                                                                                                 |
| §7.4 "Rust 单测未接默认 CI 流" | **已闭**：CI `rust` job 实跑 `cargo fmt --check` + `clippy -D warnings` + `cargo test --workspace`                                                                                                                                                                                    |
| §6.5 "提示注入只观测不阻断"    | **已阻断**：`security/promptInjectionGuard.ts` 规则式拦截（高危命中即 `blocked:true`）且接线 `agent/config/cli`。剩余缺口仅为**度量**（T4.4 数据集，挂起）                                                                                                                            |
| §6.6/6.7 "T4 未建 / T5 未建"   | **主体落地**：T4 6/7（缺 T4.4 度量）、T5 5/5（含覆盖率体检、多样性保留、退火、技能稀疏化、推理自适应）                                                                                                                                                                                |

---

## 2. 今日新结项摘要（2026-09-13，详录见两板 §13）

P 系列新结 **15** 项（P0.3 / P1.4 / P2.1–P2.3 / P3.3 / P4.1 / P4.2 / P4.3 / P4.4 / P5.4 / P5.5 / P5.6 / P8.1 / P8.3 / D5 清算）；T 系列新结 **12** 项（T1.1 / T3.2 / T3.4 / T4.1–T4.3 / T4.5–T4.7 / T5.1–T5.5）。关键度量终态：`@param` 80.1% / `@returns` 83.3% / 字段注释 87.4%（≥80% 验收达成）；公开成员 JSDoc 0/981；core↔adapters 违规 0（`--strict` 全阻断）；上帝类 0；真状态导出单例 0。单测本机 1208/1235（20 项失败均为 Node 20.1 缺 `node:sqlite` 的环境性问题，CI Node 22 全绿）。新决策 **D9**（新代码禁顶层 function）与 **D7-2**（上帝类行数改代码行口径）已登记。

---

## 3. 🟡 进行中（工作树未提交，下一步先收口）

| 项                                                                             | 证据                                                                                                                                                                                                              | 收口动作                                                                       |
| ------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| ~~**ports 域收敛（C1 的 ports 部分）**~~ **✅ 已落地**（2026-09-13 晚复核）    | `git ls-files src/ports` 实测：`intelligence`(9) / `memory`(13) / `model`(2) / `runtime`(21) / `tool`(5) + 1 散文件——与本节原描述一致，且**工作树已无对应未提交重命名**（重命名已随 p3.1 / p3.2 两笔提交入库）    | 无（C1 余量 `adapters/tool`(39) 与 `server`(39) 仍超 30，属 C1 另一半，见 §4） |
| ~~**清理临时调试文件**~~ **✅ 已清理**（2026-09-13 晚）                        | 原记 4 个，实测仓库根只剩 **2 个**：`_tmp_p32_fix.mjs`（P3.2 一次性迁移脚本）、`D:deepseekomniharness_gap_report.txt`（命令 stderr 误落盘的错误栈，503B）。二者内容已逐个 Read 核实为垃圾后删除，未跟踪文件零残留 | 无                                                                             |
| **文档整理批次（本会话产物）**                                                 | 17 份归档 `docs/archive/` + 4 份 HTML 删除 + 8 处引用修复 + `docs/README.md` / `ARCHITECTURE_AND_GAP_2026-09-13.md` 新增 + 根 README 修复                                                                         | 与代码改动**分开提交**（`docs:` 前缀一笔）                                     |
| ~~**web htm → createElement 批次（C3 部分）**~~ **✅ 已完成**（2026-09-13 晚） | `web/src` 内 `html\`\``调用点 **34 → 0**，且`htm` 绑定也一并摘除（见下方实测口径）                                                                                                                                | 与文档批次**分开提交**（`refactor(web):` 前缀）                                |

> **本批次实测口径（供复核）**：调用点数按 `git show HEAD:<f> \| grep -o 'html\`' \| wc -l` 逐文件统计（App 13 / deps 1 / format 18 / highlight 3），`deps.ts`那 1 处是`htm.bind`的导出绑定、**不是调用点**，故基线为 34 而非 35。
**等价性证据**：先用`new Function(vendor/htm.umd.js)` 实测 htm 行为——**htm 会丢弃纯空白文本子节点**（`html\`<span key=1>\n</span>\``→ 无 children），据此判断 createElement 版**不补**空白文本节点即为等价；再用 fakeReact 探针 dump`App()`树（15 节点，根为`Provider`，`div.app` 就位）与 format.ts 全部渲染函数树逐一对拍。
>
> **连带完成的三件事**：
>
> 1. **htm 绑定彻底摘除**：`ui/deps.ts` 删除 `html` 导出、`web/index.html` 删除 `vendor/htm.umd.js` 脚本标签、`react-shim.d.ts` 删除 `HtmFn` 与 `declare const htm`、`web/test/mount.test.mjs` 删除 htm 桩。至此 `web/src` 对 htm **零引用**（vendor 文件保留在盘，未删）。
> 2. **修掉一个真实存量 bug**：`tokenizeMarkdown` 每行末尾 push `{t:'plain',v:'\n'}`，而 htm 会丢弃纯空白文本节点 → **FileTab 里的 `.md` 文件所有换行丢失，整篇挤成一行**。改为把 `'\n'` 作为 span 子节点。（其他语言分词器把 `\n` 留在 plain 缓冲里，不受影响——故这个 bug 只砸 markdown。）
> 3. **护栏**：新增 `web/test/format.test.mjs`（15 用例），覆盖 format.ts 全部渲染函数 + highlight.ts 换行；**变异验证过**——把修复回退成空 span 后该用例立即变红，恢复后转绿。web 测试 38 → **53**，全绿。 |

---

## 4. 剩余任务看板（A–F 六线）

### A 线 · 兼容性硬缺口（业界标配，最高优先）

| ID                                                   | 任务                                                                                                                                                                                                         | 现状证据（9-13 实测）                                                                                                                                                                                                                                                    | 动作                                                                                                                                                                                                                                                                                                                                                                                                                                 | 可证伪验收                                                                                                                                                                                                                 | 量级                             |
| ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| **A1** ✅ **已结项（2026-09-14，D10 择优依赖首例）** | 手写实现停留在 2025-06-18；官方 `@modelcontextprotocol/sdk` 1.30.0（MIT，2026-07-27 发布）托管协议协商：**最新 2025-11-25 + 五版本向后兼容**，Streamable HTTP/OAuth/Tasks 开箱即用——自研追平边际成本不可接受 | 准入 SDK+zod（allowlist 登记+体积预算覆写）；`SdkMcpServerAdapter` 桥接 ToolPort→MCP 工具（registerTool/执行往返/失败映射）；官方 Client 双端 InMemoryTransport 往返实测 **4/4 绿**；手写 src/mcp/* 保留为旧版并行回退                                                   | 官方双端往返 4/4 ✅；多实例隔离 ✅；协议版本由官方 SDK 托管（超越原单版本手写上限）                                                                                                                                                                                                                                                                                                                                                  | 1d（SDK 路线，原估 2–3d）                                                                                                                                                                                                  |
| **A2** ✅ **已结项（2026-09-14）**                   | **多档权限：参数级规则（glob）+ profile 继承**（正交两轴与 UI 档位目录**早已存在**，故「五档可切」非本批新增；本批补的是**规则可配置化**与 **profile 继承**）                                                | 实测更正：`--sandbox`(6)×`--approval`(6)+`--escalation`+`--elevated-sandbox` 正交轴、`ApprovalTierCatalog`(5 档，`approval.tiers` RPC)、`--profile` 分层**均已存在**；真缺口 = ① `buildRuleApproval` 硬编码 3 条规则且仅 `commandPrefix` 前缀匹配 ② profile 无 `extends` | 新增 `CommandGlob`（`*`/`?` → 锚定正则、元字符字面量、空模式 fail-closed）；`ApprovalRule.commandGlob`（与 `commandPrefix` 合取）；配置段 `permission.rules`/`defaultDecision`（新 `PermissionConfigValidator` + `normalizeConfig` 严格校验、无循环依赖）；`profileLoader` 多级 `extends`（防环+深度上限 8+fail-closed）；装配 `CliArgs.permissionRules` → `buildRuleApproval`；`permission` 进 `PERSISTABLE_KEYS`（工作台可持久化） | deny glob 命中即拒 ✅（`*\| sh` 中段注入被拒）；profile 继承/多级/环/父缺失 fail-closed ✅；permission 非法配置被 `normalizeConfig` 拒 ✅；新增单测 24 例，全量 1305/1298 pass / 0 fail ✅；六门禁全绿 ✅                  | ✅ 结项（2026-09-14，`3e4131a`） |
| **A3** ✅ **已结项（2026-09-14）**                   | **shellTool 工具层纵深**（原题「改 spawn 数组」按契约修正，见下方负结果留档）                                                                                                                                | 原 `promisify(exec)` 走 shell 解释器、工具层自身零纵深；且实测**非零退出经 `exec` reject 后只剩错误文案，命令输出全部丢失**                                                                                                                                              | 新增 `shellCommandParser`（顶层切段/程序名/重定向/命令替换，不可判定即 fail-closed）+ `shellCommandPolicy`（deny/allow 名单 + 命令替换开关；默认 audit 零行为变更、enforce 命中即拒）+ `shellProcessRunner`（`spawn` 显式 argv，回传 `exitCode`/`signal`/`timedOut`/`overflowed`，shell 位置由 `ComSpec`/`SHELL` 派生）+ `shellTool` 组合三者并**修复丢输出缺陷**；策略在 `configToolRegistry` 组合根装配                            | enforce 下元字符注入（`echo ok; curl … \| sh`）与命令替换（`$(…)`）被拒且不执行 ✅；非零退出回传真实退出码 + 保留已产生输出 ✅；管道/重定向契约回归全绿 ✅；新增单测 35 例，全量 1281/1274 pass / 0 fail ✅；五门禁全绿 ✅ | ✅ 结项（2026-09-14，`57f9708`） |
| A4                                                   | 提示注入**度量**（T4.4 余项）                                                                                                                                                                                | guard 已阻断（接线 agent/config/cli）；缺数据集度量                                                                                                                                                                                                                      | 离线 curated 注入用例集（AgentDojo/InjecAgent 子集抽样的自维护快照，避免 D4 联网问题）                                                                                                                                                                                                                                                                                                                                               | 拦截率/误报率出数；基线入库                                                                                                                                                                                                | 1–2d                             |

> **A3 口径修正（负结果留档，2026-09-14）**：看板原动作「`exec`→`spawn(cmd, args, {shell:false})`（命令降级为 argv 数组）」**实测与既有契约测试冲突**——`tests/unit/shellTool.test.ts` 明确断言「shell 语义（管道与重定向）是工具契约的一部分，**不可退化为 argv 数组**」。若按字面执行，等于单方面撕毁工具对模型的承诺（模型仍会发 `a | b` 而工具已不支持），是把安全改进做成功能回归。
>
> **处置**：**不采纳**字面动作，改为「保持执行形态（shell 解释器 + 命令文本）+ 在工具内部补结构化解析与策略裁决」——同样达成 A3 的三项意图（工具层纵深 / 元字符注入可拒 / 输出与退出码语义），且不动契约。
>
> **默认 audit 是刻意的**：把默认值改成「阻断」属**行为变更**，须由 A2 多档权限显式驱动（`permissionMode`），不在 A3 里偷改默认语义。`OMNI_SHELL_POLICY=enforce` 可先行强制。

### B 线 · 可信度（对竞品可证伪）

| ID  | 任务                                 | 现状证据                                                                                         | 动作                                                                                 | 可证伪验收                              | 量级             |
| --- | ------------------------------------ | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ | --------------------------------------- | ---------------- |
| B1  | **官方 SWE-bench Verified 子集跑分** | 自研 10 题套件 live 10/10；`--swebench-remote` 钩子已备，**须 API key** ⏸️                       | 接官方数据集子集 + 常态化跑分 + 结果入 `benchmark/`                                  | 官方 Verified 子集 Pass@1 + 95% CI 出数 | 1–2d（解锁后）   |
| B2  | Terminal-Bench 子集 + 竞品横向       | 无                                                                                               | 同架构接第二轮基准                                                                   | 第二基准出数 + 与 grep 竞品同预算对照   | 2d               |
| B3  | **OS 沙箱真机验证**                  | landlock/seatbelt/bwrap 占位 fail-closed；真实生效仅 policy/restricted + Windows RestrictedToken | Linux/macOS 真机跑 `sandboxElevatedReal` + seatbelt 用例；无法验证则维持诚实降级表述 | 三后端真机各有通过/降级记录             | 0.5–1d（解锁后） |

### C 线 · 工程收口（原 P 系列遗留，§13.5）

| ID                                                                                                                                                    | 任务                                                                                                                                                                                                                                                                                                                     | 现状证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 可证伪验收                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 量级                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1                                                                                                                                                    | **目录收敛**：ports 拆分 🟡 进行中；余 `adapters/tool`(39)、`server`(39) 超 30 + spark/spill/lsp/sandbox/memory 域收敛                                                                                                                                                                                                   | arch:gate 告警                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 单目录 ≤30；同名域归属唯一                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 2–3d                                                                                                                                                                                                                                                                                                                                                                                                      |
| **C1 更新（2026-09-14 复核 → ✅ 全部达成）**                                                                                                          | **三目录全部拆完**：ports 49→五域（memory 13/runtime 20/tool 5/intelligence 9/model 2，`278ecc3`）、adapters/tool 39→九域（`ec379b0`）、server 39→三域（`51e87f1`）；lsp/code 并入适配器族、spill 策略回迁 context（`ab00d89`）；turnDiffTracker 归位（`10cc4aa`）；**arch:gate 目录告警 0 个**，全量单测 1234/1234 绿   | 无（C1 结项）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| C2                                                                                                                                                    | P4.6 类型逃逸                                                                                                                                                                                                                                                                                                            | `as unknown as` 13、非空断言 ~181                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `as unknown as`=0、`!` 减半                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 1–2d                                                                                                                                                                                                                                                                                                                                                                                                      |
| **C2 ✅ 已结项（2026-09-14，3c35dd9）**                                                                                                               | **P4.6 类型逃逸收口**：`as unknown as` 14→0（前批 40b2603）；非空断言 281→51（AST 实测基线 281，非 board 原估 ~181），引入 `src/util/arrayAt.ts` 的 `at<T>(xs: ArrayLike<T>, i)`（显式越界收窄，非逃逸）改写 src 合法下标 `X[i]!`→`at(X, i)`（类型化数组/string/元组冗余 `!` 直去；嵌套 `B[sy]![sx]!`→`at(B[sy]!, sx)`） | typecheck / lint(0 error) / check --strict / arch:gate / audit:maturity / audit:standard:delta 全绿；后端全量 1385·1378·0 fail·7 skip（无回归）                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ✅                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| C3                                                                                                                                                    | P5.1–P5.3 前端大改                                                                                                                                                                                                                                                                                                       | `App.ts` **768** 行（原 752）/69 hook；render 内联箭头 80+；**htm 调用点 34 → 0 ✅**（2026-09-13 晚）；**2026-09-13 深夜拆容器/视图两层 ✅**                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **hook=0 ✅、内联=0 ✅（htm=0 早达成）**：App.ts 768→160 行，内联 primitive hook 调用 53→0（迁 `useAppController.ts`），render 内联箭头 4→0（改 `ctrl.*` 命名回调）；`main.ts` 调用点零改动。⚠️ 代价：拆出 `useAppController`+`appReducers` 共 +13 顶层函数，C7 存量 419→~432——此增量是 React 容器/视图拆分的必然代价（hook 必须是顶层函数），非自由散函数，D9「新增为 0」口径据实修正                                                                                                                                                        | 3–5d                                                                                                                                                                                                                                                                                                                                                                                                      |
| C4                                                                                                                                                    | P6.2 封装收紧 + P8.2 API 面                                                                                                                                                                                                                                                                                              | `index.ts` 479 行/71 导出                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 出口分类清晰、beta 面收敛                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 1–2d                                                                                                                                                                                                                                                                                                                                                                                                      |
| **C4 更新（2026-09-14 复核 → 🟡 P8.2 已达成）**                                                                                                       | **P8.2 核验达成**：index.ts 已有 26 个 `@public/@beta` 分类分区且 `api:check` 全绿——「出口分类清晰」达成。**P6.2 启发式扫描 38 候选被 TS2341 全部证伪**（Phase-5 门面模式同文件外部调用所必需）→ 维持「纯人工判断 + 按模块小步推进」口径                                                                                 | P6.2 人工判断按模块推进                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| C5                                                                                                                                                    | P7/T6 栈升级                                                                                                                                                                                                                                                                                                             | tsconfig 渐进加严、verbatimModuleSyntax、ES2025、TS7 仅 noEmit                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 各项 tsc/eslint 双绿                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | 2d                                                                                                                                                                                                                                                                                                                                                                                                        |
| **C5 ✅ 已结项（2026-09-14，`7063141`）**                                                                                                             | **P7.1/2/3 栈升级收口**：P7.3 target/lib ES2022→**ES2024**（TS 5.9.3）✅；P7.2 `verbatimModuleSyntax` 启用 ✅（全库仅 1 处 type-only import 修正，`7269b5f`）；**P7.1** `exactOptionalPropertyTypes` 启用 ✅（原估 180 处，实测经「行区间批量补 `                                                                        | undefined` + 对象容器括号匹配 + 手工 Edit」收口，涉及 **89 文件 +666/−606**，无外部依赖）。**末 2 处为 fresh-literal 怪癖**：`Plugin.effect`字面量`plugin.effect?.bind(plugin)`=`(() => void                                                                                                                                                                                                                                                                                                                                                                                                                        | Promise<void>)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | undefined`，目标须写成 `effect?: (() => void                                                                                                                                                                                                                                                                                                                                                              | Promise<void>) | undefined`（括号不可省，否则 ` | undefined` 错挂到返回类型）；`SparkCycleReport.etching`容器值`X | undefined`，目标 `etching?: X`不含 undefined → 补外层`etching?: X | undefined` | typecheck / lint(0 error) / check --strict / arch:gate / audit:maturity / audit:standard:delta 全绿；web:test **110/110**；后端全量 **1385·1378·0 fail·7 skip**（无回归） | ✅  |
| C6                                                                                                                                                    | prettier 尾账                                                                                                                                                                                                                                                                                                            | src 0 欠账；余 66 处在 tests/docs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 全库 `--check` 0                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 0.5d                                                                                                                                                                                                                                                                                                                                                                                                      |
| **C6 更新（2026-09-14 复核 → ✅ 全部达成）**                                                                                                          | **prettier 尾账结项（根因修复）**                                                                                                                                                                                                                                                                                        | **口径更正**：看板原写「src 0 欠账；余 66 处在 tests/docs」，实测 `prettier --check .` 报 **2822 个文件**，其中 **2776 个（98.4%）是 EOL 伪报**（`resources/` 下 2752 个上游 vendored 文档 + 项目文件 24 个），**真实内容欠账只有 46 处**，`src/**` 确为 0（与看板一致）。**根因**：本仓此前**没有 `.gitattributes`**，Windows 上 `core.autocrlf=true` 把工作区检出为 CRLF，而 `.prettierrc.json` 要求 `endOfLine: "lf"` → **任何 Windows 检出必然全量误报**；且**不显式声明 EOL 策略时 `prettier --write` 只是暂时消音**——git 下次检出又写回 CRLF，欠账**必然复发**（实测 git 警告 "LF will be replaced by CRLF"） | ① **新增 `.gitattributes`（`* text=auto eol=lf`）**——检出恒为 LF、跨平台稳定，**根因消除**（实测 `git restore` 后文件即为 LF，此前为 CRLF）；② **`.prettierignore` 增列 `resources/`**——上游 vendored 的 ComfyUI 文档语料（3461 个已跟踪文件，非本项目源码），实测会重排 1292 个上游文档 ~82k 行、破坏与上游比对且零价值，与既有 `dist/`、`node_modules/`、`native/**` 同类；③ 46 处真实欠账 `prettier --write` 修净。**验收**：`npx prettier --check .` = **0（全库，含 resources）**；根六道门禁全 0；`npm test` 1385·1378·0 fail（无回归） | ✅                                                                                                                                                                                                                                                                                                                                                                                                        |
| **C7 存量顶层函数递减（首批 `5d4216e` + 二批 `90c30e5` + 三批 `94f9e2b` + 四批 `4d41e58` + 五批 `0b9e851` + 六批 `2c7353c`）✅ 已结项（2026-09-15）** | 存量顶层函数收敛（D9 增量）                                                                                                                                                                                                                                                                                              | 顶层纯内部 **155 → 0**（首批 155→133 计 22；二批 133→108 计 25；三批 108→91 计 17；四批 91→73 计 18；五批 73→29 计 44；六批 29→0 计 29，共 −155）；D9 新增为 0。注：前报「434/412」为含嵌套函数的旧口径，本批起统一为顶层-only 口径（279 导出不受影响）                                                                                                                                                                                                                                                                                                                                                             | typecheck / lint(0 err) / check --strict / arch:gate / audit:maturity / audit:standard:delta 全绿；全量单测 1385·1378·0 fail·7 skip（无回归）                                                                                                                                                                                                                                                                                                                                                                                                 | ✅ 已结项（六批 29→0：15 无宿主模块新建同名宿主类收口；3 个类名冲突文件重命名 codeGraph.ts→codeGraphIndex.ts / regime.ts→regimeCost.ts / worktree.ts→worktreeOps.ts 以匹配宿主类；safePolicyEvaluator.ts 4 个内部函数按语义归因到既有类 tokenize→Parser、evalAst/resolve/looseEq→SafePolicyEvaluator；ResonantFieldEngine.ts 2 个数学原函数迁入新建 adapters/memory/resonantFieldMath.ts 避免上帝类越线） |

### D 线 · Web UI 工程化（P5 之外的质量收口）

| ID                                            | 任务                                                            | 现状证据（9-13 实测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 可证伪验收                                                                                                                                                                                                                    | 量级 |
| --------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| **D1** ✅ **已结项（2026-09-14，`de10f05`）** | **a11y 基线**                                                   | **口径更正**：9-13 报告「aria 仅 7 处」已过期，实测远超。已补 NavRail `aria-current`、Toast `role=status`+`aria-live=polite`、WorkIndicator 忙碌态 live-region、ApprovalModal（`role=dialog`/`aria-modal`/`aria-labelledby`/初始焦点/Escape/焦点圈定）、StreamView `role=log`+`aria-live`。**新增 `web/test/a11yContrast.test.mjs` 对比度门禁**：解析 theme.css 双主题 token + 样式表同规则/基类合并后的 color×background 对，按 WCAG 相对亮度须 ≥4.5:1，并自检「白/白=1、黑/白=21」防假绿。门禁抓出并修掉 3 类**真实缺陷**：`--surface`/`--text-dim` 为**未定义 token**（浅色下静默退化为深色底/浅色字）、`.toast.ok/.err` 浅色无覆盖（1.20/2.03）、`.modal .allow/.deny` 深色仅 2.12/3.17 → 新增 `--on-*`/`--toast-*` token 修正                                                                                                                                                                                                                                                                                                                                                   | 对比度门禁 **5/5**；mount a11y 契约 **+9** 例；`web/test` 由 5 → **9** 个文件                                                                                                                                                 | ✅   |
| **D2** ✅ **已结项（2026-09-14，`de10f05`）** | **增量流式**                                                    | **精确缺口**：模型层 `stream()` 早已存在且 `stepRunner.requestModel` 已走 `stream()` + `live.onTextDelta`，但 web 侧 `WebLiveView` **只实现了 `onToolInput`，`onTextDelta` 从未实现 → 助手正文增量在 web 路径被静默丢弃**（工具参数增量此前已端到端通）。已补：后端 `webLiveView.onTextDelta` → `notify('thread.text_delta', {text})`；前端 `AppReducers.appendTextDelta`/`ingestEvent`（assistant 落账即转存 finalized）+ `SessionController.appendTextDelta` + `AppController` 路由 + 新 `StreamingAssistantCard`（`aria-hidden`：半句话对读屏是噪声，等事实事件落成后由最终卡片整段播报一次） + `StreamView.wasStreamed` / `AssistantCard.animate` 抑制「已逐字显完 → 归入正式卡片又从零重播」的视觉回跳                                                                                                                                                                                                                                                                                                                                                                          | `stepRunnerLiveWeb` 端到端断言每段均为**增量片段（非累积全文）**；`WebLiveView` 单测 +1；全量后端 **+3** 例                                                                                                                   | ✅   |
| **D3** ✅ **已结项（2026-09-14，`a0c860e`）** | **UI e2e / 视觉回归**（原「需 playwright 依赖准入」口径已更正） | **口径更正**：看板原写「playwright = 0 → 需依赖准入」，但 playwright/puppeteer 的体积与传递依赖远超本项目依赖预算（默认 2MB / 20 个传递依赖），且 E1 已确立「驱动本机已装浏览器」的零运行时依赖路线 → **未准入任何新依赖**，按同口径实现：零依赖 Node http 静态服务托管 `web/`（stub 页作**内存路由**注入、不落盘，保持用例只读仓库的 hermetic 属性）+ stub 在 app ESM 之前注入假 `fetch`(/rpc) 与假 `EventSource`(/events) + headless Chrome `--dump-dom` 取回交互后 DOM。覆盖关键路径 **8 步**：app 挂载 → turns.run 派发 → SSE 连上 → 流式卡可见 → 审批弹窗可见 → 审批已响应 → 产物卡可见 → 最终助手卡，缺任一步即红；未找到浏览器时**显式 skip**（不伪装成通过）。stub 内置**自诊断**（页面错误栈 / console.error / RPC 清单 / `#root` 内容长度），失败时随断言消息一并输出。**并借此抓出真实阻断缺陷**：`App.ts` 的 `extends ReactComponent` 继承的是 `react-shim.d.ts` 的**环境声明类**（只有类型、无运行时值）→ 浏览器里 `ReactComponent is not defined`，`#root` 恒空，**工作台自 C3（`5301c6e`）起在浏览器中无法挂载**，已改为 `extends React.Component`（详见 §5 第 7 条） | `web/test/e2e.test.mjs` 稳定通过（8/8 步）；`web:test` **99/99**；`web/test` 由 9 → **10** 个文件                                                                                                                             | ✅   |
| **D4** ✅ **已结项（2026-09-14，`de10f05`）** | **原生弹窗 → 应用内对话框**（原「alert → Toast」口径已更正）    | **口径更正**：实测 `web/src` 下 `window.alert` **已 0 命中**（C3 重构时已清，看板原「7 处」为过期数字）；真正的原生阻塞弹窗是 **6 处 `window.confirm` + 1 处 `window.prompt`**，且二者是**决策闸门**（返回布尔/字符串），换 Toast 会造成**静默放行 / 静默中止**的真实回归 → 故做**应用内对话框**而非 Toast。已补 `web/src/core/DialogService.ts`（confirm/prompt 返 Promise；未绑定宿主时 confirm 抛错 fail-closed，绝不静默当取消）+ `web/src/ui/components/DialogHost.tsx`（`role=dialog`/`aria-modal`/`aria-labelledby`/初始焦点/Escape/Enter/Tab 焦点圈定/卸载聚焦还原）+ 替换 ChangesTab×2·GraphTab·MemoryTab·ProfilesTab·RollbackTab·AddMenu 共 **7 处**                                                                                                                                                                                                                                                                                                                                                                                                                       | 新增 `web/test/noNativeDialogs.test.mjs` 回归护栏（web/src 再现 confirm/prompt/alert 即失败）；`dialogService` **10** 例                                                                                                      | ✅   |
| **D5** ✅ **已结项（2026-09-14，`a0c860e`）** | **artifact 画廊/分享**                                          | **口径更正**：看板旧名 `artifactFromTool` 早已不存在，实现为 `ArtifactResolver.fromTool`（原仅覆盖 write_file / apply_patch 两类）。已扩到 **7 类产物型工具**（write_file / apply_patch / sketch_write / read_file / list_dir / lsp 系列等），路径字段**别名归一**（path / file / file_path / target / filename），非法入参 **fail-closed**；`ArtifactCard` 统一「打开 / 下载」通路（下载经 `/files?path=` 编码，预览经 `onOpen` 面板）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `web/test/artifactResolver.test.mjs` **11** 例（含别名归一与非法入参）；`web:build` 0 错                                                                                                                                      | ✅   |
| D6                                            | web 测试扩容                                                    | 4→**5** 个测试文件（models/mount/textUtils/uiModels/+appReducers）→ **本轮增至 10 个**（+dialogService / noNativeDialogs / a11yContrast / artifactResolver / e2e）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **✅ 已达成**：`web/test/appReducers.test.mjs`（12 用例，零 DOM）锁控制器归约（事件累加=StreamView 数据源 / 工具结果·增量 / graph 进度·完成 / 会话合并 / finalText 兜底 / 命令面板）；D3 e2e 补齐「真实浏览器里真能跑」这一层 | 1d   |

### E 线 · 能力扩展（差异化）

| ID                                            | 任务                                         | 现状证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 可证伪验收                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 量级 |
| --------------------------------------------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| E1                                            | **浏览器验证 / computer use**                | 全仓零（**D3 已落地其前置：零依赖驱动本机 Chrome 的静态服务 + `--dump-dom` 回环**）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | CDP 驱动本机已装浏览器（零运行时依赖路线）：截图→视觉核对→操作回环一例                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 3–5d |
| **E2** ✅ **已结项（2026-09-14，`e2ebea8`）** | **A2A 跨进程/WebSocket 实测（口径更正）**    | **口径更正**：看板原写「localhost 回环 100%（传输+协议层）」，并把它读作「只差跨机」——实测发现**更大的问题：A2A 在生产装配层根本不可达**。`a2a` 字段只在 `OmniHarnessConfig` 上声明，`ConfigFactory.build` 的返回**显式对象字面量**未透传它 → `runtime` 的 `if (config.a2a?.enabled === true)` **恒为假**，server/client 永不装配；且 CLI / 配置文件 / `KNOWN_KEYS` **三处均无入口**，即便透传也无从设置。U6 回环脚本直接 import `a2a` 模块、绕过了装配层，故长期未暴露（与 D2/D3/D4/F3/F5/E3 同一形态）。**另抓出两处真实缺陷**：① **断言形态不匹配**——客户端发的是 `AgentIdentityPort.authorizationHeader()` 产物（`AgentAssertion <信封>`），而 `A2aServer` 按**裸信封**解码 → **真实 Ed25519 身份下验签恒失败**，仅自造桩身份（自带去前缀逻辑）侥幸通过；② `WsConnection` 未监听 socket `error` → 一次 `ECONNRESET` 即以**未捕获异常**炸掉宿主进程。                                                                                             | ① 装配透传 `a2a: partial.a2a`；② 生产入口：`FileConfig.a2a` + `KNOWN_KEYS` + CLI `--a2a` / `--a2a-port` / `--a2a-peer` / `--a2a-transport http\|ws`（枚举白名单 fail-closed）；③ 新增**零依赖 WebSocket 传输** `src/a2a/wsA2aTransport.ts`（复用 `WsConnection` 帧编解码；客户端帧按 RFC6455 掩码；服务端仅接受 `/a2a-ws` Upgrade），`a2a.transport` 可切换（缺省 http，零破坏）；④ 断言前缀归一（裸信封与 `AgentAssertion ` 头形态均接受，验签仍严格）；⑤ socket `error` 收敛为幂等关闭；⑥ 传输 SSRF 判定做 `ws(s)→http(s)` 协议映射（否则 `ws://` 被判「非 HTTP(S) 协议」而**静默不发帧**，表现为调用侧超时）；⑦ `listen()` 返回 `server.address()` 的**实际**端口（支持临时端口）。**测试** `tests/unit/a2aCrossProcess.test.ts`（**5** 例，其中 3 例真跨进程）：子进程内起真实 `A2aServer`，委托经 `SubagentRuntimeFactory` + 真实 `Agent` 执行，**输出携带子进程 pid → 证伪「同进程伪造」**；HTTP 与 WebSocket 各一例；真实 Ed25519 跨进程验签 fail-closed 一例；装配透传 + 运行时真装配一例；缺省关零破坏一例。**门禁**：根六道全 0；`npm test` **1385 / 1378 pass / 0 fail / 7 skip**（+8 例）。 | ✅   |
| **E3** ✅ **已结项（2026-09-14，`cfcab80`）** | **U4 RLVR autoRun 实跑（口径更正）**         | **口径更正**：看板原写「默认关、端到端未开」——「默认关」属实且是**正确设计**（零破坏旁路）；但「端到端未开」的根因**不是缺实现**（RLVR 全机器 `RlvrLoop`/`verifiableRewardForCode`/`modelRlvrSampler`/`createRlvrEvolutionController` 早已实现且有组件级单测），而是**三层接线断裂**：① `ConfigFactory.build` 返回的 `ResolvedConfig` 字面量**未透传** `evolutionRlvr` → 调用方设置即被静默丢弃、`createRuntime` 恒读不到；② `createRlvrEvolutionController` 默认门禁写死 `moireEnergy(c.skill, 32)`，而该判据（单技能≈0.28 / 组合≈0.45+）只在 **n=64** 成立（= `moireComposer` 自身默认场边长 `fieldSize ?? 64`、两个既有门禁测试所用 N、`benchmark.ts` 文档所载实测量级）→ n=32 下组合仅≈0.041、增益≈0.033 < 默认 `minGain` 0.05 → **默认门禁恒不通过、RLVR 阶段永远到不了**；③ `evolutionRlvr.minGain` 早已声明并文档化，但 runtime 未透传、`RlvrEvolutionOptions` 无此选项 → 死旋钮。与 D2/F3/F5 同一形态：**能力在，是接线断 + 看板证据过期**。 | ① 装配透传（`evolutionRlvr: partial.evolutionRlvr`）；② 默认场边长改命名常量 `DEFAULT_MOIRE_FIELD_SIZE = 64`，与全局规范尺度一致（**非**调整 `moireEnergy` 本身或阈值）；③ 补 `RlvrEvolutionOptions.minGain` + runtime 透传。**生产入口（此前为零）**：CLI `--evolution-rlvr` / `--rlvr-verify`（含 `%CODE_FILE%`）/ `--rlvr-samples` / `--rlvr-min-reward` / `--rlvr-candidates` / `--rlvr-min-gain` / `--rlvr-auto-run` ＋ `omniharness.json` 的 `evolutionRlvr` 对象段（`FileConfig` + `KNOWN_KEYS` + `configDefaults` 映射）；缺省关，未给 verify 时奖励恒 0（fail-closed 安全旁路）。测试 `tests/unit/evolutionRlvrWiring.test.ts`（**9** 例）：含**真实子进程** `node --check %CODE_FILE%` 的「采样→验证→绿样本回放」闭环一例、fail-closed 红样本不进回放、经 `createRuntime().cycle()` 的端到端（绿→晋升 / 全红→RLVR 否决）、argv 与配置文件双入口。**门禁**：根六道全 0；后端全量 **1377 / 1370 pass / 0 fail / 7 skip**（+9 例，无回归）                                                                                                                                                       | ✅   |
| E4                                            | **AST 图上下文压缩深化**（战略三硬骨头之一） | repoMap/contextEngine/layeredCodeGraph 已落地（层化图 AB 实测 −9.1pp 判负留档）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | 以「图上下文」为主线做压缩增量：同 corpus AB ≥ +5pp 或诚实判负留档；复用 D6 两关流程                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 3–5d |

### F 线 · 测试与运维债

| ID                                            | 任务                                   | 现状证据（9-13 实测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 可证伪验收                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 量级                  |
| --------------------------------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- |
| **F1** ✅ **已结项（2026-09-14，`a0c860e`）** | **后端零测试模块 ×3 → ×2**（口径更正） | **口径更正**：原写 `cliDataCmds`(569) / `configBuilders`(252) / `registrySources`(293) 三模块「测试引用 = 0」，实测均已过期：`cliDataCmds` **早已有** `tests/unit/cliDataCmds.test.ts`（端到端 3 例）；行数亦不准（`cliDataCmds` 实为 105 行；无 `configBuilders.ts`，实为 `src/config/configBuilder.ts` 337 行）。**真缺口 = 2 个**：`src/config/configBuilder.ts`（各 `buildXxx` 分支与默认值回落）与 `src/plugin/registrySources` 族（shared / localDir / bundled / remoteHttp / fileRegistry 的解析与失败降级；注入 fetcher 桩，零联网）                                                                                           | 两模块各有单测：`configBuilder` **16** 例 + `registrySources` **13** 例；全量后端 **+29** 例（1308 → 1337）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ✅                    |
| F2                                            | 本机 Node 统一 22                      | **（2026-09-14 更新：便携版 Node v22.14.0 已部署于 `D:\deepseek\node-v22.14.0-win-x64\`，全量单测 1234/1241 — 0 失败）** 余：系统 PATH 正式安装（当前便携版不入 PATH）                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 本机全量单测 0 环境性失败（已达成，待 PATH 化）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | ✅（余 0.1d PATH 化） |
| **F3** ✅ **已结项（2026-09-14，`85b5090`）** | **vault 接线 + 故障熔断（口径更正）**  | **口径更正一**：原写「vault 端口在、实现薄」不成立——`CryptoVault`（AES-256-GCM 加密落盘 + 主密钥三级级联 env→密钥文件→自动生成）、`EnvVault`（免落盘回退）与 `tests/unit/vault.test.ts` 契约测试均齐备。**真缺口是接线**：保险库此前只被 `vault` 子命令使用，而模型适配器（`buildModel`）与模型路由（`buildRouter`）都**硬读 `process.env`**——存在保险库里的凭据在生产路径上无人读取（与 D2「模型层 `stream()` 早已在、web 侧 `onTextDelta` 未实现」同一形态）。**口径更正二**：「无熔断」属实——全仓仅有**成本**熔断（`costBudget`，语义是「别花钱」），**故障**熔断（「别打已经挂掉的下游」）此前为零                                 | 熔断：三态状态机（closed/open/half-open，注入时钟、冷却到期惰性转半开、探测全成功复位 / 任一失败立即重开路、开路期 `allowRequest()` 拒绝且**不发起调用**）+ `CircuitOpenError` 可辨识拒绝（带 `breaker`/`openedAt`/`retryAfterMs`）+ `CircuitBreakingModel` 装饰器 + 装配顺序 `Budgeted(Circuit(Retrying(inner)))`（一次逻辑调用 = 一次熔断计数）+ 配置文件/CLI 双向可调（默认**开**）。凭据：`CredentialResolver`（env 优先、库回退、仅填充未设置项、读取失败 fail-closed）+ `buildConfig` 装配期水合（**早于 `buildModel`**）+ `--vault-hydrate`（缺省关 = 零行为变更；缺主密钥来源时显式跳过，避免 CryptoVault 凭空落新密钥文件）                                                                                                       | ✅                    |
| F4                                            | OIDC 真实 IdP 验证                     | 仅 mock IdP                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | 对真实 IdP（或 keycloak 容器）走通 PKCE 全流程                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 0.5d（解锁后）        |
| **F5** ✅ **已结项（2026-09-14，`56b2ea9`）** | **体验收尾（口径更正）**               | **口径更正**：看板原写「无命令面板/快捷键」不成立——**命令面板早已完整落地**：`CommandPalette.tsx`（class 组件，`role=dialog`/`listbox`/`option`、初始焦点、↑↓/Enter/Esc 键盘导航）+ `CommandPaletteModel.ts`（过滤/夹取/移动纯逻辑，6 例单测）+ **全局快捷键 `Ctrl/Cmd+P` 与 `Ctrl/Cmd+K` 唤起**（`AppController.keyHandler`）+ TopBar `⌘` 入口 + 16 条命令（11 面板 + 5 动作）+ 完整 `.cmdk-*` 样式——与 D2/D4/F3 同一形态：**能力早就在，是看板证据过期**。**真缺口两处**：① `CommandItem.group` 虽已存在并被 `filter` 命中，却在**渲染时被丢弃**（面板平铺、无分组标题），`hint` 亦全为空串（提示形同虚设）；② `llms.txt` 全仓零命中 | 命令面板：`group` 真正被 UI 消费——新增 `CommandPaletteModel.grouped(query)`（分组**不改变键盘选中的扁平语义**：条目 `index` 恒等于 `filter()` 扁平下标，单测锁死）+ `CommandPalette` 按 `role=group` 渲染分组标题。快捷键**单一来源**：新增 `KeyboardShortcuts`（`resolve` 与 `label` 共用同一份绑定表，杜绝「提示 Ctrl+B、实际没绑定」）+ `ShortcutActions`（解析与执行分离），扩展 `Ctrl/Cmd+N`（新会话）/ `Ctrl/Cmd+B`（会话面板）/ `Ctrl/Cmd+Shift+E`（工具面板）/ `Ctrl/Cmd+Shift+L`（主题）；`buildCommands` 的 `hint` 引用同源文案。llms.txt：按 llmstxt.org 规范发布 `docs/llms.txt` 并登记 `docs/README.md` §1。**门禁**：`web:build` 0 错 / `web:test` **110/110**（测试文件 11→**13**）/ 根六道全 0 / 后端全量 1368·1361·0 fail | ✅                    |

---

## 5. 下一批建议（ROI 排序）

1. **C3 前端大改 + D6 护栏已结项**（2026-09-13 末）：App.ts 768→160 行 class 组件、内联 primitive hook 53→0、render 内联箭头 4→0；按编码技能改**标准 class 形态**（AppController 门面 + Session/Composer/Graph 三子控制器 + AppReducers 纯归约类，替代 useAppController hook 与 appReducers 自由函数），**零 function 关键字**、新文件零 `audit:standard:delta` 违规、全门禁绿；+appReducers 测试 12 例（现 import 类）解 §6 挂起；代码批次（`d08c109` hook 版、`5301c6e` class 版）已提交，本看板随文档批次一并提交。
2. ~~**A3 shellTool**~~ / ~~**A1 MCP 升级**~~ / ~~**A2 多档权限**~~ / ~~**D4 + D1 + D2**~~ / ~~**D3 + D5 + F1**~~ / ~~**F3 vault 接线 + 故障熔断**~~ / ~~**F5 体验收尾**~~ / ~~**E3 U4 RLVR autoRun 实跑**~~ / ~~**E2 A2A 跨进程/WebSocket 实测**~~ / ~~**C6 prettier 尾账**~~ / ~~**C5 exactOptional 180 处**~~ ✅ **均已结项**（2026-09-14，`de10f05`、`a0c860e`、`85b5090`、`56b2ea9`、`cfcab80`、`e2ebea8`、`456db8a` 与 `7063141` 与 `3c35dd9`）——**A 线、D 线、C 线（C2/C5/C6）的可开工项均已清空，E 线 E3/E2 亦已收口**。**下一批建议**：C7 存量顶层函数递减 → C4 P6.2 人工判断；**F4（OIDC 真实 IdP 验证）本批实测被外部前提阻断**（0.5d 规格，但**需容器起 keycloak，本机 docker / podman 均不可用**）→ 维持 §6 挂起；E1（浏览器验证/computer use）与 E4（AST 图上下文压缩深化）为 3–5d 级战略项，宜单独排期。
3. **本批提交口径说明**：D4/D1/D2 原拟各一笔提交，但三件在 `AppController` / `App` / `StreamView` / `layout.css` / `theme.css` 上**深度交织**，机械切分会使中间态过不了门禁 → 按仓库既有纪律合成**一笔代码**（`de10f05`，36 文件 +1369/−58）＋一笔看板（本节），提交信息内分节说明（D4/D1/D2 各一节）。
4. **F1 零测试模块（2d）+ F2 Node 统一（0.1d）**——测试覆盖补残段。
5. ~~C1 余量~~ / ~~C2 非空断言减半~~ / ~~C5 exactOptional 180 处~~ **✅ 均已达成**（2026-09-14 复核）；**C7 存量顶层函数递减已结六批（155→0，`5d4216e`+`90c30e5`+`94f9e2b`+`4d41e58`+`0b9e851`+`2c7353c`，共 −155）——C7 线正式收官**；机械阶段 + 无宿主模块专项（五批收 44 个）+ 六批（余 29 个清零：ResonantFieldEngine 2 个迁入新建 resonantFieldMath.ts、safePolicyEvaluator 4 个归因到既有类、23 个小无宿主模块函数收口）主体全部完成，**C4 P6.2 人工判断项同步清空**。
6. 挂起组（解锁即做）：B1 官方跑分（等 key）→ A4 注入度量（等数据集快照）→ B3 沙箱真机（等 Linux/macOS）。
7. **本批发现并修复的真实阻断缺陷（D3 的附带产出，优先级等同一次 P0 修复）**：`web/src/ui/App.ts` 根组件写作 `extends ReactComponent` —— 继承的是 `web/src/types/react-shim.d.ts` 里的**环境声明类**，它只提供类型、**没有运行时值**：类型检查全绿、单测全绿，但浏览器执行到模块求值时即抛 `ReactComponent is not defined`，`App.mount` 之前就中断，`#root` 恒为空 → **工作台自 C3 重构（`5301c6e`）起在浏览器中无法挂载**。本工程既有约定是 `extends React.Component<P, S>`（`AppComponent` 即如此），`App.ts` 是唯一异类；之所以长期未被发现，是因为此前 web 侧全部是「组件级 render 断言」，**从不加载真实入口**，而 `web/dist/` 被 gitignore、不进代码评审 —— 这正是 D3 e2e 存在的价值。修法：改为 `extends React.Component<Record<string, never>, AppState>`，并同步把 `typeof ReactComponent.prototype` 改为 `typeof React.Component.prototype`；产物 `web/dist/ui/App.js` 由 `extends ReactComponent` 变为 `extends React.Component`。**提交口径**：D3/D5/F1 三件在同一批 e2e 调试中交叉（`App.ts` 的修复是 D3 的前置），故合成**一笔代码**（`a0c860e`，8 文件 +1025/−18）＋一笔看板（本节）。
8. **本批口径更正与提交口径（F3，85b5090）**：① 看板原写 F3「vault 实现薄」——实测不成立，真实缺口是**接线**（保险库凭据在生产凭据链上无人读取）；已在 F3 行内留档更正。② 「无熔断」属实，但需区分**成本熔断**（已有，语义「别花钱」）与**故障熔断**（本批新增，语义「别打已经挂掉的下游」），二者不可互替。③ 提交口径：熔断与 vault 接线在 `cliBuildConfig` / `configBuilder` / `configError` / `argParser` / `cliFlagTable` 上深度交织（前者加配置字段与旗标，后者在 `buildConfig` 同处接线），机械切两笔会让中间态过不了门禁 → 按仓库既有纪律合成**一笔代码**（19 文件 +1424/−10）＋一笔看板（本节）。④ 设计取舍：`--vault-backend env` 对水合是**恒等空操作**（源即 `process.env`），故**不提供**该旋钮——无用旋钮比没有旋钮更糟；水合源固定为加密保险库。

9. **本批口径更正与提交口径（F5，`56b2ea9`）**：
   ① 看板原写「无命令面板/快捷键」——实测**命令面板早已完整落地**（`CommandPalette.tsx` class 组件 + `CommandPaletteModel.ts` 纯逻辑 + **全局快捷键 `Ctrl/Cmd+P` / `Ctrl/Cmd+K`** + TopBar `⌘` 入口 + 16 条命令 + 完整 `.cmdk-*` 样式 + a11y `role=dialog/listbox/option`），与 D2「模型层 `stream()` 早就在、web 侧未实现 `onTextDelta`」、D4「`window.alert` 早已清零」、F3「vault 实现完备、缺的是接线」**同一形态：能力在，是看板证据过期**。**真缺口两处**：`CommandItem.group` 虽已存在并被 `filter` 命中，却**在渲染时被丢弃**（面板平铺、无分组标题），`hint` 亦全为空串；`llms.txt` 全仓零命中。
   ② 本批实质增量：`CommandPaletteModel.grouped()`（分组**不改变键盘选中的扁平语义**——条目 `index` 恒等于 `filter()` 扁平下标，单测锁死）+ `role=group` 分组渲染；新增 `KeyboardShortcuts`（`resolve` 与 `label` **同一份绑定表**，杜绝「提示与实际绑定漂移」）与 `ShortcutActions`（解析与执行分离，各自可单测），扩展 `Ctrl/Cmd+N` / `Ctrl/Cmd+B` / `Ctrl/Cmd+Shift+E` / `Ctrl/Cmd+Shift+L`；`docs/llms.txt` 按 llmstxt.org 规范发布并登记进 `docs/README.md` §1。
   ③ 过程留档（红线被如实拦下）：新增快捷键动作的初版把 `runShortcut` 直接写在 `AppController` 内 → **成员数 24→26，越过 ≤25 上帝类红线**，pre-commit 增量门禁如实中止提交；按「找职责缝抽协作者」范式改为 `ShortcutActions` + 收进既有 `children` 组合（**零新增属性/方法**，成员数回到 24）后通过。
   ④ 另一踩坑（门禁读盘口径）：`audit:standard:delta` 读的是**暂存区（index）**而非工作区——修复工作区文件后**必须重新 `git add`**，否则门禁仍按旧暂存内容判红（本次实测复现：工作区已 24 成员，因暂存区仍是 26 成员版本而持续报错）。

10. **本批口径更正与提交口径（E3，`cfcab80`）**：
    ① 看板原写 E3「默认关、端到端未开」——「默认关」属实且是**正确设计**（零破坏旁路）；但「端到端未开」的根因**不是缺实现**（RLVR 全机器早已在且有组件级单测），而是**三层接线断裂**：`ConfigFactory.build` 静默丢弃 `evolutionRlvr`；默认门禁场边长写死 32（与 `moireComposer` 默认 64 及 `benchmark.ts` 文档实测量级不符 → 门禁恒不通过、RLVR 阶段不可达）；`minGain` 声明未接线（死旋钮）。与 D2「`stream()` 早在而 web 未实现 `onTextDelta`」、D3「`App.ts` 继承环境声明类致整站挂载阻断」、D4「`alert` 早已清零」、F3「vault 完备缺接线」、F5「命令面板早已完整」**同一形态：能力在，是接线断 / 看板证据过期**。
    ② **度量口径说明（D7）**：本批**未**修改 `moireEnergy` 函数本身或其阈值；改的是「默认场边长 32→64」——原值 32 与系统自身规范尺度矛盾（`moireComposer` 默认 `fieldSize ?? 64`、两个既有门禁测试均用 N=64、`benchmark.ts` 文档自述的 0.28 / 0.45+ 即 n=64 实测量级），故属**修错**而非「为达标改口径」。
    ③ 提交口径：三处缺陷位于同一链路（装配 → 门禁尺度 → 入口），机械切分会使中间态无意义 → 合成**一笔代码**（`cfcab80`，9 文件 +413/−1）＋一笔看板（本节）。
11. **本批口径更正与提交口径（E2，`e2ebea8`）**：
    ① 看板 E2 原证据「localhost 回环 100%（传输+协议层）」**本身属实**，但把它读作「只差跨机」是**误读**——真正的阻断在**装配层**：`a2a` 从未被 `ConfigFactory.build` 透传（死特性），且 CLI / 配置文件 / `KNOWN_KEYS` 三处入口皆缺。**教训**：回环 / 基准脚本直接 import 目标模块会**绕过装配层**，从而给出一条「功能已通」的**假绿灯**；验收必须走生产装配路径（`ConfigFactory.build` + `createRuntime`）。
    ② 本批顺带修的 3 类真实缺陷都属「测试盲区品类」：**断言形态不匹配**（自造桩身份自带去前缀 → 掩盖真实身份下验签恒失败）、**socket `error` 未处理**（一次 RST 即崩宿主进程）、**9 个取值旗标漏登记 `VALUE_FLAGS`**（`--rlvr-*` ×5 来自 E3、`--a2a-*` ×3 本批新增、`--network-allow` / `--events` / `--model-circuit-breaker-*` ×2 来自 F3；取值会被 `collectPositional` 误判为位置参数并入 `prompt`）。末者的护栏已**机器化**（`tests/unit/cliFlagValueRegistry.test.ts` 把「消费取值 ⇒ 必须登记」定为不变量，新增旗标漏登记即红）——正是该护栏抓出额外 4 个历史漏登记。
    ③ 提交口径：装配透传 / 生产入口 / WS 传输 / 断言归一 / 测试夹具在同一条链路上深度交织，机械切分会使中间态过不了门禁 → 按仓库既有纪律合成**一笔代码**（`e2ebea8`，16 文件 +981/−25）＋一笔看板（本节）。
12. **本批口径更正与提交口径（C6，`456db8a`）**：
    ① 看板原写 C6「src 0 欠账；余 66 处在 tests/docs」「全库 `--check` 0」——**低估且归因错误**。实测 `prettier --check .` 报 **2822 个文件**，其中 **2776 个（98.4%）是 EOL 伪报**（`resources/` 下 2752 个上游 vendored 文档 + 项目文件 24 个）；**真实内容欠账只有 46 处**，`src/**` 确为 0（与看板一致）。
    **根因**：本仓此前**没有 `.gitattributes`**，而 Windows 上 `core.autocrlf=true` 会把工作区检出为 CRLF；`.prettierrc.json` 又要求 `endOfLine: "lf"` → **任何 Windows 检出都必然全量误报**。更关键：**不显式声明 EOL 策略的话，`prettier --write` 只是暂时消音**——git 下次检出又把文件写回 CRLF，欠账**必然复发**（git 实测警告 "LF will be replaced by CRLF"）。这也是该「尾账」长期名列看板却始终「差一点」的机械原因。
    ② 本批实质增量：**新增 `.gitattributes`（`* text=auto eol=lf`）**——检出恒为 LF、跨平台稳定，**根因消除**（实测 `git restore` 后文件即为 LF，此前为 CRLF）；**`.prettierignore` 增列 `resources/`**——那是**上游 vendored 的 ComfyUI 文档语料**（3461 个已跟踪文件，非本项目源码），实测格式化会重排 1292 个上游文档 ~82k 行、**破坏与上游的对齐比对能力且对项目零价值**，与既有 `dist/`、`node_modules/`、`native/**` 属同类（`.prettierignore` 开篇即写「不应被 Prettier 改写，否则污染 diff 且无意义」）；46 处真实欠账 `prettier --write` 修净。
    **口径说明（D7）**：排除 `resources/` **不是**放松项目格式标准——看板自身证据（「余 66 处在 tests/docs」）本就只统计**项目自有文件**、从未把 vendored 语料计入（否则数字应是 2822 而非 66）；此改动是把**机器检查对齐到看板既有意图**。项目自有文件（含 46 处欠账）**全部** `prettier --check` 通过，验收仍为「全库 `--check` 0」。
    ③ 提交口径：`.gitattributes` / `.prettierignore` / 46 处格式修复同属一条链路（先定 EOL 与范围，再修内容），机械切分会使中间态再次全量误报 → 合成**一笔代码**（`456db8a`，49 文件 +2043/−1016）＋一笔看板（本节）。

---

## 6. 挂起条件清单

| 任务              | 缺的外部条件                                                                                                                       | 解锁动作                                                |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| B1 官方 SWE-bench | API key（D4 禁付费进**主门禁**，跑分属报告级不受限）                                                                               | 提供 key → `--swebench-remote` 常态化                   |
| A4 注入度量       | AgentDojo/InjecAgent 数据集（D4 禁联网进主门禁）                                                                                   | 离线 curated 快照自维护（A4 已按此改写，可提前）        |
| B3 沙箱真机       | Linux / macOS 环境                                                                                                                 | 真机跑 `sandboxElevatedReal` + seatbelt；或维持诚实降级 |
| F4 OIDC 真机      | 真实 IdP 或本地 keycloak                                                                                                           | 容器起 keycloak 走 PKCE                                 |
| C3 前端大改       | ~~web 挂载测试护栏扩容（D6 前置）~~ **已解除**（D6+appReducers 护栏 2026-09-13 深夜落地）；**✅ 已结项**（2026-09-13 末 class 化） | 先 D6 再 C3                                             |

---

## 7. 与旧看板及文档的关系

| 文档                                 | 关系                                                                                               |
| ------------------------------------ | -------------------------------------------------------------------------------------------------- |
| `REFACTOR_BOARD_2026-09-12.md`       | **历史账**：P/T 批次执行记录 + 决策日志（D1–D9 沿用）+ 度量口径权威（§1）。剩余任务已并入本板 C 线 |
| `UPGRADE_BOARD_2026-09-12.md`        | **历史账**：U1–U7 能力升级记录。剩余任务已并入本板 B/E 线                                          |
| `ARCHITECTURE_AND_GAP_2026-09-13.md` | 差距全景（§1 的五条更正待其下次修订同步）                                                          |
| `docs/README.md`                     | 文档索引；本板登记于"旗帜文档"                                                                     |
| 治理规则                             | 沿用 `docs/README.md` 五条铁律：状态只认本板；一主题一权威；负结果留档；带日期即快照               |
