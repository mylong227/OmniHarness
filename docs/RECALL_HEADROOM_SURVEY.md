# 检索命中率提升空间调研（2026-09-17）

回答「**还能不能提升命中率，还有多少优化空间**」。结论全部来自真实 `src/` 语料（482 文件 / 7748 符号）

- 真实生产 `query()` 的实测，不引用未验证的估计。

## 1. 结论速览

| 项                                    | 数值                   | 说明                                                   |
| ------------------------------------- | ---------------------- | ------------------------------------------------------ |
| 当前命中率（**旧**生产默认 fileK=10） | **51.5%**              | hitRate@10，33 条锚点查询                              |
| 可达天花板（排序/融合手段上限）       | **81.8%**              | 受词法盲区限制，非 100%；**对抗口径**下的上界，见 §8.2 |
| **排序/融合可吃空间**                 | **30.3pp**（10/33 条） | GT 在候选池内但没进 Top-10                             |
| 须语义路才能突破                      | 18.2pp（6/33 条）      | GT 与查询**零词法交集**，池再大也捞不回                |

**核心判断：还有很大空间（+30.3pp 可达），且主要卡在「预算 × 重排」这一被低估的旋钮上，不是缺新算法。**

> **2026-09-17 更新：该判断已兑现。** `fileK=14 + 精排默认开` 已翻为生产默认 ⇒ 对抗口径命中率
> **51.5% → 69.7%**（+18.2pp，CI [54.5, 84.8]，生产入口等价性验收 33/33）。详见 §8.3。
> 同批还系统证伪了「蜘蛛网/图检索」五形态（§8），并澄清了本报告天花板的**口径归属**（§8.2）。

## 2. 失败模式诊断（`evals/headroom-analysis.mjs`）

33 条查询按「GT 文件在深层候选池中的最佳排位」分类：

- **OK 17 条（51.5%）** — 已命中
- **RANKING 10 条（30.3%）** — GT 在候选池但排不进 Top-10
  - 排位分布：**11–20 位 5 条、21–50 位 4 条、51–100 位 1 条**
  - ⇒ **9/10 只需把 GT 提升 1–40 位即可救回**，属浅层排序问题，非深水区
- **LEXICAL 6 条（18.2%）** — GT 与查询零词法交集（如 "chain of thought persisted to disk" → `StoredTrace`），
  BM25 候选池里**根本不存在**该文件

Oracle 上界（完美重排）：候选深度 D=20 → 66.7%，**D=50 → 78.8%，D≥100 → 81.8%（饱和）**。
即：**候选深度 50 已覆盖几乎全部可达空间**，再深无意义。

## 3. 已验证**无效**的路径（避免重复投入）

| 假设                     | 实测结果                                                                        | 证据                         |
| ------------------------ | ------------------------------------------------------------------------------- | ---------------------------- |
| 扩大候选池深度（20→482） | **完全无效**：池 37.6→202.7（5.4×），命中率 45.5%→45.5% 纹丝不动                | `evals/pool-depth-probe.mjs` |
| LSA 潜语义扩展           | 召回持平 67.0%，符号精确率 25.5%→10.5% 腰斩                                     | 历史消融                     |
| 图检索（graph）          | 净负面：图排序对查询不敏感（Top-14 跨查询重合度 0.936 vs BM25 0.058）= 常量偏置 | `evals/rank-veto-retro.mjs`  |

⇒ **「捞更多候选」这条路已被证伪；瓶颈在排序质量与词法鸿沟，不在池子大小。**

## 4. 已验证**有效**的路径

### 4.1 预算 × 重排组合（**最大发现，免模型**）

reranker 在 fileK=10 时只能排 10 个候选、施展不开；预算放大后增益显著放大：

| fileK              | 基线  | +rerank   | 相对生产默认 | 命中率 CI95 | 判定             |
| ------------------ | ----- | --------- | ------------ | ----------- | ---------------- |
| 5（P5 降档档）     | 39.4% | 36.4%     | **−12.1pp**  | [24.2–57.6] | ⚠️ 降档代价      |
| **10（生产默认）** | 51.5% | 54.5%     | —            | [33.3–69.7] | 基线             |
| **14**             | 60.6% | **69.7%** | **+18.2pp**  | [54.5–84.9] | ✅ CI 下界超基线 |
| **20**             | 66.7% | **75.8%** | **+24.2pp**  | [60.6–87.9] | ✅ CI 下界超基线 |

**fileK=14 + rerank 命中率 69.7%，CI 下界 54.5% > 基线均值 51.5% ⇒ 两关第一关通过**
（历史 `evals/rerank-ab.mjs` 亦记录该档两关全过：+9.6pp、CI95 [1.80, 18.60]pp 不跨 0、留出折 38/40 为正）。

代价：repo-map 文件数 10→14（token ↑40%）；准确度 9.4%→9.3%（几乎不变），召回 26.7%→39.4%（+12.7pp）。

### 4.2 PRF / RM3（已落地，opt-in）

`evals/recall-precision.mjs`：fileK=10 档 +3.0pp（51.5%→54.5%），与 rerank 叠加 +6.1pp。
**n=33 下 CI 重叠 ⇒ 未达翻默认阈值**，故保持 `OMNI_RM3=1` opt-in、默认关。

### 4.3 语义路（理论最大，当前**环境阻塞**）

历史实测（recall@14，同 33 查询）：

| 模型                      | 混合命中  | 相对 BM25(43.3%) |
| ------------------------- | --------- | ---------------- |
| MiniLM-L6-v2 (384d, 22MB) | 59.4%     | +16.1pp          |
| e5-base-v2 (768d)         | 57.6%     | +14.3pp          |
| **e5-large-v2 (1024d)**   | **64.5%** | **+21.2pp**      |

**但当前网络无法落地**：huggingface.co 超时、hf-mirror.com 403、ghproxy 代理 HF 亦 403，
且代理环境变量已消失（此前为 `127.0.0.1:20816`）。语义路适配器已完整接线
（`transformersEmbeddingAdapter.ts` + `@huggingface/transformers` 4.2.0 + onnxruntime 1.24.3 已装），
**只差模型文件**，网络恢复或人工投放模型后即可启用。

## 5. 建议（按性价比排序）

1. **翻 fileK=14 + rerank 为默认** —— **✅ 已执行（2026-09-17）**：唯一已通过统计显著性的免模型提升
   （+18.2pp，CI 下界超基线），代价 token ↑40%。生产入口验收 33/33、旋钮可变性 5/5，
   `fileK` 默认 **10 → 14**、`rerank` 默认 **关 → 开**（`repoMapContextEngine.DEFAULT_FILE_K`）。
   详见 §8.3 与 `evals/production-defaults-check.mjs`。
2. **重估 P5 降档** —— 降档把 fileK 缩到 5，实测命中率 **−12.1pp（51.5%→39.4%）**，
   此前只记了 token 收益、未量化质量代价。建议降档改为「只关语义路、不缩 fileK」，或缩到 8 做折中（待测）。
3. **网络恢复后启用语义路** —— 唯一能吃 18.2pp 词法盲区的手段，预计再 +16~21pp。
4. **扩语料** —— 当前 n=33 使 CI 偏宽（±18pp）。语料扩到 n≥80 可让 6.1pp 级增益也可判定。

## 6. 复现命令（全部免网络、免模型）

```bash
npm run build
node evals/headroom-analysis.mjs        # 失败模式诊断 + oracle 上界
node evals/pool-depth-probe.mjs         # 候选池深度扫描（证伪用）
node evals/budget-recall-tradeoff.mjs   # 预算×重排 权衡矩阵 + bootstrap CI
node evals/recall-precision.mjs         # 生产口径四项指标（含 PRF 对照）
# —— 2026-09-17 追加：生产默认档验收 + 蜘蛛网五形态 ——
node evals/production-defaults-check.mjs  # 生产入口等价性/可变性 + 新默认档命中率 + 自然口径
node evals/spider-reach-probe.mjs         # ① 图可达性与 oracle 上界
node evals/spider-pool-ab.mjs             # ② 扩张池 + 精排
node evals/spider-density-sweep.mjs       # 稀疏度扫描（鉴别力 vs 漏网）
node evals/spider-quota-ab.mjs            # ③ 配额保留 / 能量项
node evals/spider-termweb-ab.mjs          # ④ 词项共现网扩展
node evals/spider-final-ab.mjs            # ⑤ 形态丝 + 对抗/自然口径对照
```

## 7. 诚实边界

- 所有数字为 hitRate@K（「至少命中 1 个相关文件」的查询占比），非端到端任务成功率；
  命中率提升不等于最终 agent 效果提升，后者需 P6 官方基准解锁后验证。
- 33 条查询的 CI 宽度约 ±18pp，故只把「CI 下界超基线」的档位判为达标，其余一律不翻默认。
- 语义路数字为历史 recall@14 口径，与本调研 hitRate@10 口径不同，跨平台比较时已标注。

---

## 8. 蜘蛛网（图/网检索）五形态：全部证伪，机理已定位（2026-09-17 追加）

针对「网如天网无处不在、无处躲藏」「中央蜘蛛精确捕食」的检索构想，按「**先验证空间、再写生产代码**」
纪律建 5 个离线评测脚本（免网络免模型）逐形态实测。**五形态全负，故不落任何生产代码**——避免给自己
加一份看不见的噪声税。这是负结果，但它的价值在于**防止未来重复投入**。

| #   | 形态                   | 实现要点                                              | 实测                                                | 证据                           |
| --- | ---------------------- | ----------------------------------------------------- | --------------------------------------------------- | ------------------------------ |
| ①   | 文件级 PPR 单路排序    | 稀有共享标识符边 + 同目录弱边；个性化重启 = BM25 种子 | 浅层 o@14 最高 **51.5% < 池 60.6%**；D≥100 才 84.8% | `evals/spider-reach-probe.mjs` |
| ②   | 扩张候选池 + 既有精排  | PPR 新猎物追加池尾，交 `FileReranker` 重排            | **+0.0pp**（N 10→80 纹丝不动）                      | `evals/spider-pool-ab.mjs`     |
| ③   | 配额保留 / 蜘蛛能量项  | 留 q 个名额给网黏来的新猎物；或把 PPR 分作精排第三项  | **−6.1 ~ −15.2pp**                                  | `evals/spider-quota-ab.mjs`    |
| ④   | 词项共现网（PMI）扩展  | 节点=词、边=语料级共现、激活传播选扩展词              | **+0.0 ~ −6.1pp**                                   | `evals/spider-termweb-ab.mjs`  |
| ⑤   | 形态丝（字符前缀桥接） | 专治缩写/形态鸿沟（`characters` → `chars`）           | **−6.1 ~ −12.1pp**                                  | `evals/spider-final-ab.mjs`    |

### 8.1 共同机理（本节最有价值的产出）

1. **网无法同时做到「全覆盖」与「有鉴别力」。** 稠密档（df≤4 ⇒ 482 节点 / 16034 边 / 平均度 **66.5**）
   的 1 跳邻域就吃掉全网 **90.2%**、2 跳 100% ⇒ 处处等价、没有信息量；稀疏档（每节点 top-8）1 跳覆盖
   掉到 90.9% ⇒ 漏网。`evals/spider-density-sweep.mjs` 扫了 4×3×3 组配置，**不存在甜点**。
2. **网的拓扑类型错了。** 文件级网连的是「**结构性**关联」（两文件共享罕见标识符），而词法盲区要的是
   「**语义**关联」——`StoredTrace` ← "chain of thought **persisted** to disk"、`Canonicalizer` ←
   "what **normalizes** text"。**结构边在语义鸿沟上够不到，这不是调参能解决的。**
3. **零依赖的分布统计吃不到语义红利。** 词级共现网选出的扩展词是「共现显著但**不同义**」的长尾词
   （`semweight` / `turnoutcome` / `modulus`），与 LSA 证伪（精确率腰斩）、PRF 仅 +3pp 同源。
4. 这是**第 4 次独立确认「扩大可达集」对本语料无效**（前有 `pool-depth-probe.mjs`、
   `rank-veto-retro.mjs`，以及本节的 ①②③）。

### 8.2 附带的口径澄清（重要，回答「能不能到 100」）

本套 33 条查询是**刻意避开锚点字面词**的**对抗样本**（见 `evals/recall-precision.mjs` 注释）。同批锚点换
**自然提问方式**后：

| 口径                             | 命中率@14 | CI95         |
| -------------------------------- | --------- | ------------ |
| 对抗口径（本调研全程使用的口径） | 69.7%     | [54.5, 84.8] |
| **自然口径（锚点 + 自然语言）**  | **97%**   | [90.9, 100]  |
| **直接指名（只给符号名）**       | **97%**   | [90.9, 100]  |

⇒ **生产现实下检索已近饱和**；本报告第 1 节那个 81.8% 是**对抗口径**的上界，**不是生产现实的上界**。
自然口径仅剩 **1 条**未命中（`Diagnostics` ← "where do language server error reports come from"）。
证据：`evals/production-defaults-check.mjs`、`evals/spider-final-ab.mjs`。

### 8.3 已执行：预算 × 精排翻默认（第 5 节建议 1）

`fileK` **10 → 14**、`rerank` **默认关 → 默认开**（两者同一决策，见 §4.1）。生产入口等价性 **33/33**、
旋钮可变性 **5/5**、命中率 **51.5% → 69.7%**（+18.2pp，CI [54.5, 84.8]）。
验收脚本：`evals/production-defaults-check.mjs`（走生产入口本体，不用原型自证）。
