[package]
name = "omni-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "OmniHarness Rust 内核 CLI：exec / tools / run / approval / context / sdk"

[dependencies]
omni-core = { path = "../omni-core" }
omni-sdk = { path = "../omni-sdk" }
serde_json = "1"
