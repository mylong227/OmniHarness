# Runtime and legacy evaluator dependencies for CPython 3.10.x and CUDA 11.3 wheels.
# The installed NVIDIA driver may be newer, but it must support CUDA 11.3.
# GenEval2 uses a separate modern PyTorch environment; see BENCHMARKS.md.
--extra-index-url https://download.pytorch.org/whl/cu113
--find-links https://download.openmmlab.com/mmcv/dist/cu113/torch1.12.0/index.html

openai-codex==0.147.0
PyYAML==6.0.2
numpy==1.26.4
Pillow==11.3.0
opencv-python-headless==4.10.0.84
requests==2.32.5
tqdm==4.67.1
scipy==1.15.3

torch==1.12.1+cu113
torchvision==0.13.1+cu113
torchmetrics==1.3.2
# CLIP support for the legacy PyTorch stack. GenEval2 requires transformers 4.57.0
# and PyTorch >=2.2 in its separate environment, not this requirements file.
transformers==4.30.2
accelerate==0.33.0
open-clip-torch==2.24.0
clip-benchmark==1.6.1

mmcv-full==1.7.2
mmdet==2.28.2
