# 编译产物 / 生成物（不应被 Prettier 改写，否则污染 diff 且无意义）
dist/
coverage/
native/**/*.js
native/**/*.d.ts
web/
*.min.js
*.map

# 上游 vendored 语料（第三方文档/数据，非本项目源码）
# 不应被本仓 Prettier 改写：实测会重排 1292 个上游文档（~82k 行）的列表缩进与空行，
# 既污染 diff、又破坏与上游的对齐比对能力，对项目零价值（2026-09-14，C6 实测）。
resources/

# 依赖与锁文件
node_modules/
package-lock.json

# 日志
*.log

# 工作区元信息
.workbuddy/

# harness 运行时产物（Terminal-Bench 临时工作树 / 模型缓存 / 探针输出）与 Rust 构建产物：
# 不是本仓库维护的源码，体量大且含不可读的缓存目录（Prettier 走进即 EPERM）。
.omniharness/
.omni-worktrees/
target/

# 评测生成的报告 JSON（机器写出、非人工维护）：格式化它们只会让 `format:check` 随机变红，
# 且每次跑评测都产生无意义 diff（2026-09-22 扩容评测集时实测 2 份报告即触发红灯）。
*.report.json

# 本机外部产物（非本仓库生成）：Chrome profile/注册表备份，含二进制与锁定文件，Prettier 扫描即报错。
chrome-hijack-backup/

